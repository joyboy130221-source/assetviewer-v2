import { useEffect,useState,type ReactNode } from 'react';import { NavLink,useLocation,useNavigate } from 'react-router-dom';import { api } from '../services/api';import type { AdminUser } from '../types';import { useAppUI } from './AppUI';
const links=[['/admin','Dashboard',''],['/admin/maximo-environments','Maximo API Endpoint','maximoEnvironments'],['/admin/api-logs','API Request Logs','apiLogs'],['/admin/roles','Roles','roles'],['/admin/users','Users','users']] as const;
export function AdminLayout({permission,eyebrow,title,subtitle,children}:{permission?:string;eyebrow:string;title:string;subtitle?:ReactNode;children:ReactNode}){const [user,setUser]=useState<AdminUser|null>(null),[denied,setDenied]=useState(false);const nav=useNavigate(),loc=useLocation(),ui=useAppUI();useEffect(()=>{api<{user:AdminUser}>('/api/auth/me',{cache:'no-store'}).then(b=>{if(permission&&!b.user.permissions?.[permission])setDenied(true);else setUser(b.user)}).catch(()=>nav(`/login?next=${encodeURIComponent(loc.pathname+loc.search)}`));},[permission]);if(denied)return <main className="page-shell"><section className="state-card error"><h2>Access denied</h2><p>Your role does not have permission to open this page.</p><a className="secondary-button" href="/admin">Back</a></section></main>;if(!user)return <main className="page-shell"><section className="state-card"><span className="spinner"/> Loading administration…</section></main>;const logout=async()=>{if(await ui.confirm({title:'Sign out?',message:'Your administration session will be closed.',confirmText:'Sign Out'})){await fetch('/api/auth/logout',{method:'POST'});nav('/login')}};return <div className="admin-shell"><aside className="admin-sidebar"><div className="admin-brand">Asset Viewer Admin</div><nav className="admin-nav">{links.filter(([, ,p])=>!p||user.permissions?.[p]).map(([to,label])=><NavLink key={to} to={to} end={to==='/admin'}>{label}</NavLink>)}</nav><button onClick={logout} className="secondary-button">Sign Out</button></aside><main className="admin-main"><div className="admin-topbar"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1>{subtitle&&<p className="subtitle">{subtitle}</p>}</div><span>{user.name} • {user.role}</span></div>{children}</main></div>}
