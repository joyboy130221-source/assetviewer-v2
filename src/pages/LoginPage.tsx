import { useState,type FormEvent } from 'react';import { useNavigate,useSearchParams } from 'react-router-dom';import { api,jsonInit } from '../services/api';import { LoadingOverlay } from '../components/AppUI';
export default function LoginPage(){const [msg,setMsg]=useState(''),[loading,setLoading]=useState(false);const [sp]=useSearchParams(),nav=useNavigate();const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setLoading(true);setMsg('');try{const fd=new FormData(e.currentTarget);await api('/api/auth/login',jsonInit('POST',Object.fromEntries(fd)));nav(sp.get('next')||'/admin')}catch(e:any){setMsg(e.message)}finally{setLoading(false)}};return <><div className="login-page"><section className="login-card"><p className="eyebrow">ASSET VIEWER ADMINISTRATION</p><h1>Sign in</h1><p className="subtitle">Use your administrator account to manage users, roles and Maximo environments.</p><form onSubmit={submit}><div className="form-field"><label>Username or Email</label><input name="login" autoComplete="username" required/></div><div className="form-field"><label>Password</label><input name="password" type="password" autoComplete="current-password" required/></div>{msg&&<p className="form-message error-message">{msg}</p>}<button className="primary-button">Sign In</button></form></section></div><LoadingOverlay show={loading} text="Signing in…"/></>}
