import { useEffect,useState,type FormEvent } from 'react';import { AdminLayout } from '../../components/AdminLayout';import { useAppUI } from '../../components/AppUI';import { api } from '../../services/api';
const pretty=(v:any)=>JSON.stringify(v??null,null,2);
export default function ApiLogsPage(){const[logs,setLogs]=useState<any[]>([]),[envs,setEnvs]=useState<string[]>([]),[detail,setDetail]=useState<any>(null),ui=useAppUI();const load=async(form?:HTMLFormElement)=>{const p=form?new URLSearchParams(new FormData(form) as any):new URLSearchParams();[...p].forEach(([k,v])=>!v&&p.delete(k));const b=await api<any>(`/api/admin/api-logs?${p}`);setLogs(b.data);setEnvs(b.environments)};useEffect(()=>{load().catch(e=>ui.toast(e.message,'error'))},[]);const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();load(e.currentTarget).catch(x=>ui.toast(x.message,'error'))};return <AdminLayout permission="apiLogs" eyebrow="DIAGNOSTICS" title="API Request Logs" subtitle="Trace Maximo requests and responses. Credentials are automatically masked; only a short fingerprint is retained for troubleshooting."><section className="data-card"><form className="log-filters" onSubmit={submit}><label>Environment<select name="env"><option value="">All environments</option>{envs.map(x=><option key={x}>{x}</option>)}</select></label><label>Result<select name="result"><option value="">All</option><option value="success">Success</option><option value="error">Error</option></select></label><label>Method<select name="method"><option value="">All methods</option>{['GET','POST','PUT','PATCH','DELETE'].map(x=><option key={x}>{x}</option>)}</select></label><label>Status<input name="status" placeholder="e.g. 400"/></label><label className="log-search">Search URL / error<input name="search" placeholder="mxasset, worklog, error..."/></label><div className="inline-actions log-actions"><button className="primary-button">Apply Filter</button><button type="reset" className="secondary-button">Clear</button><button type="button" className="secondary-button" onClick={()=>load().catch(e=>ui.toast(e.message,'error'))}>Refresh</button></div></form><div className="table-wrap"><table className="admin-table log-table"><thead><tr><th>Time</th><th>Environment</th><th>Method</th><th>Request</th><th>Status</th><th>Duration</th><th>Result</th><th/></tr></thead><tbody>{logs.map(x=>{let path=x.request_url;try{const u=new URL(x.request_url);path=u.pathname+(u.search||'')}catch{}return <tr key={x.id}><td>{new Date(x.created_at).toLocaleString()}</td><td>{x.environment_name||'—'}</td><td><span className="method-badge">{x.request_method}</span></td><td className="request-cell">{path}</td><td><span className={x.success?'badge-active':'badge-inactive'}>{x.response_status??'NETWORK'}</span></td><td>{x.duration_ms==null?'—':`${x.duration_ms} ms`}</td><td>{x.success?'Success':x.error_message||'Error'}</td><td><button className="secondary-button compact-button" onClick={()=>setDetail(x)}>View</button></td></tr>})}</tbody></table></div>{!logs.length&&<p className="subtitle">No API logs match the current filter.</p>}</section>{detail&&<div className="modal-backdrop log-modal" onMouseDown={e=>e.target===e.currentTarget&&setDetail(null)}><div className="modal-card log-detail-card"><div className="log-detail-head"><div><p className="eyebrow">API TRACE</p><h2>{detail.request_method} • Log #{detail.id}</h2></div><button className="secondary-button" onClick={()=>setDetail(null)}>Close</button></div><div className="log-meta">{[['Environment',detail.environment_name],['Status',detail.response_status??'No response'],['Duration',detail.duration_ms==null?'—':`${detail.duration_ms} ms`],['Created',new Date(detail.created_at).toLocaleString()],['Result',detail.success?'Success':'Error']].map(([a,b])=><div key={a}><span>{a}</span><strong>{b}</strong></div>)}</div>{[['Request URL',detail.request_url],['Request Headers',detail.request_headers],['Request Parameters',detail.request_params],['Request Body',detail.request_body],['Response Headers',detail.response_headers],['Response Body',detail.response_body],...(detail.error_message?[['Error Message',detail.error_message]]:[])].map(([t,v]:any)=><section className={`log-json-section${t==='Error Message'?' log-error':''}`} key={t}><div className="log-section-title"><h3>{t}</h3><button className="secondary-button compact-button" onClick={()=>navigator.clipboard.writeText(typeof v==='string'?v:pretty(v)).then(()=>ui.toast(`${t} copied.`))}>Copy</button></div><pre>{typeof v==='string'?v:pretty(v)}</pre></section>)}</div></div>}</AdminLayout>}
