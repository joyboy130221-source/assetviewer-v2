import { useEffect,useState,type FormEvent } from 'react';import { AdminLayout } from '../../components/AdminLayout';import { LoadingOverlay,useAppUI } from '../../components/AppUI';import { api,jsonInit } from '../../services/api';
export default function EnvironmentsPage(){const[data,setData]=useState<any[]>([]),[edit,setEdit]=useState<any>(null),[loading,setLoading]=useState(false),ui=useAppUI();const load=()=>api<any>('/api/admin/environments').then(b=>setData(b.data));useEffect(()=>{load().catch(e=>ui.toast(e.message,'error'))},[]);const save=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const b:any=Object.fromEntries(new FormData(e.currentTarget));b.active=b.active==='true';const editing=!!b.id;if(!await ui.confirm({title:editing?'Update environment?':'Create environment?',message:`Save configuration for “${b.env_name}”?`,confirmText:editing?'Update':'Create'}))return;setLoading(true);try{const r=await api<any>('/api/admin/environments',jsonInit(editing?'PUT':'POST',b));setEdit(null);await load();ui.toast(r.message)}catch(e:any){ui.toast(e.message,'error')}finally{setLoading(false)}};const del=async(x:any)=>{if(!await ui.confirm({title:'Delete environment?',message:`Delete Maximo environment “${x.env_name}”?`,confirmText:'Delete',danger:true}))return;setLoading(true);try{const r=await api<any>('/api/admin/environments',jsonInit('DELETE',{id:x.id}));await load();ui.toast(r.message)}catch(e:any){ui.toast(e.message,'error')}finally{setLoading(false)}};return <><AdminLayout permission="maximoEnvironments" eyebrow="MAXIMO CONFIGURATION" title="Maximo Environments" subtitle={<>Select the configuration from public pages using the <strong>env</strong> URL parameter.</>}><section className="data-card"><form key={edit?.id||'new'} className="admin-form" onSubmit={save}><input type="hidden" name="id" defaultValue={edit?.id||''}/><label>Environment Name<input name="env_name" defaultValue={edit?.env_name||''} required/></label><label>Description<input name="description" defaultValue={edit?.description||''}/></label><label className="wide">Maximo API Endpoint<input name="endpoint" type="url" defaultValue={edit?.endpoint||''} required/></label><label>API Key<input name="api_key" type="password" placeholder={edit?'Leave blank to keep current key':'Required for new'} required={!edit}/></label><label>Active<select name="active" defaultValue={String(edit?.active??true)}><option value="true">Yes</option><option value="false">No</option></select></label><div className="wide inline-actions"><button className="primary-button">Save Environment</button><button type="button" className="secondary-button" onClick={()=>setEdit(null)}>Clear</button></div></form><div className="table-wrap"><table className="admin-table"><thead><tr><th>Environment</th><th>Description</th><th>Endpoint</th><th>API Key</th><th>Status</th><th>Actions</th></tr></thead><tbody>{data.map(x=><tr key={x.id}><td><strong>{x.env_name}</strong></td><td>{x.description||'—'}</td><td className="endpoint">{x.endpoint}</td><td>{x.api_key_masked}</td><td><span className={x.active?'badge-active':'badge-inactive'}>{x.active?'Active':'Inactive'}</span></td><td><div className="inline-actions"><button className="secondary-button" onClick={()=>{setEdit(x);scrollTo({top:0,behavior:'smooth'})}}>Edit</button><button className="secondary-button danger" onClick={()=>del(x)}>Delete</button></div></td></tr>)}</tbody></table></div></section></AdminLayout><LoadingOverlay show={loading}/></>}
