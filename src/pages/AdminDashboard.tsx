import { AdminLayout } from '../components/AdminLayout';export default function AdminDashboard(){return <AdminLayout eyebrow="ADMINISTRATION" title="Configuration"><section className="data-card"><h2>Welcome</h2><p className="subtitle">Manage Maximo environments, application roles and users. Menu visibility is controlled by role permissions, so more administration pages can be added later without changing the role model.</p></section></AdminLayout>}
