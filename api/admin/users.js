const {query,hashPassword}=require('../../lib/db');const{requireAuth}=require('../../lib/auth');
module.exports=async(req,res)=>{const me=await requireAuth(req,res,'users');if(!me)return;try{if(req.method==='GET'){const [u,r]=await Promise.all([query(`SELECT u.id,u.username,u.name,u.email,u.active,u.role_id,r.name role_name,u.created_at,u.updated_at FROM app_users u JOIN app_roles r ON r.id=u.role_id ORDER BY u.name`),query(`SELECT id,name FROM app_roles WHERE active=TRUE ORDER BY name`)]);return res.json({data:u.rows,roles:r.rows})}const b=req.body||{};if(req.method==='POST'){if(!b.username||!b.name||!b.password||!b.role_id)return res.status(400).json({error:'Username, Name, Password and Role are required.'});await query(`INSERT INTO app_users(username,name,email,password_hash,active,role_id) VALUES($1,$2,$3,$4,$5,$6)`,[b.username.trim(),b.name.trim(),b.email?.trim()||null,hashPassword(b.password),b.active!==false,b.role_id]);return res.json({message:'User created successfully.'})}if(req.method==='PUT'){if(Number(b.id)===Number(me.id)&&b.active===false)return res.status(400).json({error:'You cannot deactivate your current user.'});let sql=`UPDATE app_users SET username=$1,name=$2,email=$3,active=$4,role_id=$5,updated_at=NOW()`;const p=[b.username.trim(),b.name.trim(),b.email?.trim()||null,b.active!==false,b.role_id,b.id];if(String(b.password||'').trim()){sql+=`,password_hash=$7`;p.push(hashPassword(b.password))}sql+=` WHERE id=$6`;await query(sql,p);return res.json({message:'User updated successfully.'})}if(req.method==='DELETE'){if(Number(b.id)===Number(me.id))return res.status(400).json({error:'You cannot delete your current user.'});await query('DELETE FROM app_users WHERE id=$1',[b.id]);return res.json({message:'User deleted successfully.'})}res.status(405).json({error:'Method not allowed'})}catch(e){res.status(e.code==='23505'?409:500).json({error:e.code==='23505'?'Username or Email already exists.':e.message})}};
