const {query}=require('../../lib/db');const{requireAuth}=require('../../lib/auth');
const KNOWN=['roles','users','maximoEnvironments','apiLogs'];function perms(v){const p={};KNOWN.forEach(k=>p[k]=Boolean(v?.[k]));return p}
module.exports=async(req,res)=>{const user=await requireAuth(req,res,'roles');if(!user)return;try{if(req.method==='GET'){const r=await query('SELECT id,name,description,active,permissions,created_at,updated_at FROM app_roles ORDER BY name');return res.json({data:r.rows,availablePermissions:[{key:'roles',label:'Role Page'},{key:'users',label:'User Page'},{key:'maximoEnvironments',label:'Maximo API Endpoint Page'},{key:'apiLogs',label:'API Request Log Page'}]})}const b=req.body||{};if(req.method==='POST'){if(!b.name)return res.status(400).json({error:'Role Name is required.'});await query(`INSERT INTO app_roles(name,description,active,permissions) VALUES($1,$2,$3,$4::jsonb)`,[b.name.trim(),b.description||'',b.active!==false,JSON.stringify(perms(b.permissions))]);return res.json({message:'Role created successfully.'})}if(req.method==='PUT'){await query(`UPDATE app_roles SET name=$1,description=$2,active=$3,permissions=$4::jsonb,updated_at=NOW() WHERE id=$5`,[b.name.trim(),b.description||'',b.active!==false,JSON.stringify(perms(b.permissions)),b.id]);return res.json({message:'Role updated successfully.'})}if(req.method==='DELETE'){if(Number(b.id)===Number(user.role_id))return res.status(400).json({error:'You cannot delete the role used by your current session.'});await query('DELETE FROM app_roles WHERE id=$1',[b.id]);return res.json({message:'Role deleted successfully.'})}res.status(405).json({error:'Method not allowed'})}catch(e){res.status(e.code==='23503'?409:e.code==='23505'?409:500).json({error:e.code==='23503'?'This role is assigned to one or more users.':e.code==='23505'?'Role Name already exists.':e.message})}};
