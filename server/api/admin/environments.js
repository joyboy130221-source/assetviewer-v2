const {query,encryptSecret,decryptSecret}=require('../../lib/db');const{requireAuth}=require('../../lib/auth');
module.exports=async(req,res)=>{const user=await requireAuth(req,res,'maximoEnvironments');if(!user)return;try{if(req.method==='GET'){const r=await query(`SELECT id,env_name,description,endpoint,active,created_at,updated_at,api_key FROM maximo_environments ORDER BY env_name`);return res.json({data:r.rows.map(x=>{const key=decryptSecret(x.api_key);delete x.api_key;return {...x,api_key_masked:key?'••••••••'+key.slice(-4):''}})})}const b=req.body||{};if(req.method==='POST'){if(!b.env_name||!b.endpoint||!b.api_key)return res.status(400).json({error:'Environment Name, Endpoint and API Key are required.'});await query(`INSERT INTO maximo_environments(env_name,description,endpoint,api_key,active) VALUES($1,$2,$3,$4,$5)`,[b.env_name.trim(),b.description||'',b.endpoint.trim().replace(/\/$/,''),encryptSecret(b.api_key.trim()),b.active!==false]);return res.json({message:'Maximo environment created successfully.'})}if(req.method==='PUT'){if(!b.id)return res.status(400).json({error:'id is required'});const params=[b.env_name.trim(),b.description||'',b.endpoint.trim().replace(/\/$/,''),b.active!==false,b.id];let sql=`UPDATE maximo_environments SET env_name=$1,description=$2,endpoint=$3,active=$4,updated_at=NOW()`;if(String(b.api_key||'').trim()){sql+=`,api_key=$6`;params.push(encryptSecret(b.api_key.trim()))}sql+=` WHERE id=$5`;await query(sql,params);return res.json({message:'Maximo environment updated successfully.'})}if(req.method==='DELETE'){await query('DELETE FROM maximo_environments WHERE id=$1',[b.id]);return res.json({message:'Maximo environment deleted successfully.'})}return res.status(405).json({error:'Method not allowed'})}catch(e){res.status(e.code==='23505'?409:(e.status||500)).json({error:e.code==='23505'?'Environment Name already exists.':e.message})}};
